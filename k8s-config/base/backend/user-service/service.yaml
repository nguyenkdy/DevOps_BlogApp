apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: production
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 9002
      targetPort: 9002
  type: ClusterIP
