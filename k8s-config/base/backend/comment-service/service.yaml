apiVersion: v1
kind: Service
metadata:
  name: comment-service
  namespace: production
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    app: comment-service
spec:
  selector:
    app: comment-service
  ports:
    - protocol: TCP
      port: 9004
      targetPort: 9004
  type: ClusterIP
